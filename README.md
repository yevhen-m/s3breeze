# s3breeze

#### Installation

```
$ git clone https://github.com/yevhen-m/s3breeze
$ virtualenv -p <path_to_python3.6+> venv
$ source venv/bin/activate
$ python -m pip install .
$ which s3breeze && which s3cmd
<script_path>
$ # link these scripts to your PATH
```
